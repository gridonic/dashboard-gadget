# Dashboard Gadget SERVER

## Pre installation
### MongoDB

    `$ brew update`

    `$ brew install mongodb`

### NPM concurrently

`$ npm install -g concurrently`

## Installation
`$ npm install`

## Run Server
`$ npm start`